Instructions de compilations :

g++ -std=c++11 -fPIC -O3 -march=native -Wall  main.cpp -fopenmp -ltbb -o launcher.exe

Exécuter :
./launcher.exe > image.ppm

Choisir le moteur TBB, de préférence

Et par défaut :
light_list.txt

object_list.txt

Puis ouvrir :
image.ppm
